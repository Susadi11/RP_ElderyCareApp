package com.rp_elderycareapp.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Modern Blue-themed Color Palette for Elderly Care App
 * Apple-aesthetic with medical-grade trust and calm vibes
 */
object AppColors {
    // Primary Colors - Blue Scheme (Updated with custom colors)
    val Primary = Color(0xFF2196F3)           // Medium blue #2196F3
    val DeepBlue = Color(0xFF2563A8)          // Softer medium-dark blue for text
    val LightBlue = Color(0xFF90CAF9)         // Soft light blue
    val SkyBlue = Color(0xFF87CEEB)           // Sky blue
    val Azure = Color(0xFF2196F3)             // Azure (matching primary)
    val LightGray = Color(0xFFF1F5F9)         // Light gray shade
    val LightLavender = Color(0xFFE8F4FD)     // Very light blue (replacing lavender)
    val LightPeach = Color(0xFFE3F2FD)        // Very light blue (replacing peach)

    // Background Colors
    val Background = Color(0xFFFFFFFF)        // White background
    val BackgroundAlt = Color(0xFFF8FAFC)     // Alternative very light gray background
    val CardBackground = Color(0xFFFFFFFF)    // White for cards
    val CardBackgroundAlt = Color(0xFFF8FAFC) // Very light gray for cards

    // Text Colors
    val PrimaryText = Color(0xFF334155)       // Dark slate (softer than navy, less harsh contrast)
    val SecondaryText = Color(0xFF64748B)     // Blue-gray
    val TertiaryText = Color(0xFF94A3B8)      // Light blue-gray
    val BlackAsh = Color(0xFF1E293B)          // Black-ash color
    val WhiteAsh = Color(0xFFF8FAFC)          // White-ash color

    // Interactive Elements
    val PositiveTrend = Color(0xFF2196F3)     // Blue for positive trends
    val NegativeTrend = Color(0xFF90CAF9)     // Light blue for neutral trends
    val AccentBlue = Color(0xFF2196F3)        // Azure accent

    // Surface Colors
    val Surface = Color(0xFFFFFFFF)           // White surface
    val SurfaceVariant = Color(0xFFF1F5F9)    // Very light gray surface

    // Shadows (for subtle elevation)
    val ShadowColor = Color(0xFF2563A8)       // Softer blue for shadows (with alpha applied)

    // Legacy colors (kept for compatibility)
    val SoftBlue = Color(0xFF2196F3)          // Medium blue
    val SoftGreen = Color(0xFF52A48E)         // Muted teal-green (less saturated)
    val MutedRed = Color(0xFFCB7070)          // Soft rose-red (less saturated)
    val WarmBeige = Color(0xFFD6EAF8)         // Very light blue (replaces warm beige for blue theme)
}
